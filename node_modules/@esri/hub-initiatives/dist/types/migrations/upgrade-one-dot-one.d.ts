import { IInitiativeModel } from "@esri/hub-common";
/**
 * Apply the 1.0 --> 1.1 Migration to an Initiative Model
 *
 * @param model
 * @param portalUrl
 * @protected
 */
export declare function upgradeToOneDotOne(model: IInitiativeModel, portalUrl?: string): IInitiativeModel;
/**
 * As part of the 1.1 migration, we add a set of default image
 * resources into the hash.
 *
 * @protected
 * @param {IInitiativeModel} model
 * @param {string} [portalUrl]
 * @returns {IInitiativeModel}
 */
export declare function addDefaultResources(model: IInitiativeModel, portalUrl?: string): IInitiativeModel;
/**
 * Construct the url for a resource. This is specific to the migration otherwise
 * if would be hoised into a more generic module
 *
 * @protected
 * @param {string} itemId
 * @param {string} resourceName
 * @param {string} [portal]
 * @param {string} [folder]
 * @returns {string}
 */
export declare function getResourceUrl(itemId: string, resourceName: string, portal?: string, folder?: string): string;
