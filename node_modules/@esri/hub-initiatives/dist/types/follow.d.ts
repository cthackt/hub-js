import { IRequestOptions } from "@esri/arcgis-rest-request";
import { UserSession } from "@esri/arcgis-rest-auth";
import { IUser } from "@esri/arcgis-rest-portal";
export interface IFollowInitiativeOptions extends IRequestOptions {
    initiativeId: string;
    authentication: UserSession;
}
export declare const currentlyFollowedInitiatives: (user: IUser) => string[];
export declare const isUserFollowing: (user: IUser, initiativeId: string) => boolean;
/**
 * ```js
 * import { followInitiative } from "@esri/hub-initiatives";
 * //
 * followInitiative({
 *   initiativeId,
 *   authentication
 * })
 *   .then(response)
 * ```
 * Follow an initiative.
 */
export declare function followInitiative(requestOptions: IFollowInitiativeOptions): Promise<{
    success: boolean;
    username: string;
}>;
/**
 * ```js
 * import { unfollowInitiative } from "@esri/hub-initiatives";
 * //
 * unfollowInitiative({
 *   initiativeId,
 *   authentication
 * })
 *   .then(response)
 * ```
 * Un-follow an initiative.
 */
export declare function unfollowInitiative(requestOptions: IFollowInitiativeOptions): Promise<{
    success: boolean;
    username: string;
}>;
