import { IRequestOptions } from "@esri/arcgis-rest-request";
import { IInitiativeModel } from "@esri/hub-common";
import { IInitiativeGroupIds } from "./templates";
/**
 * Activate an Initiative
 * Creates an instance of an Initiative, based on an Initiative Template.
 *
 * @export
 * @param {string | any} template Initiative Template item or Id
 * @param {string} title
 * @param {any} groupIds hash of group props and ids
 * @param {IRequestOptions} requestOptions
 * @returns {Promise<IInitiativeModel>}
 * @private
 */
export declare function activateInitiative(template: string | any, title: string, groupIds: IInitiativeGroupIds, requestOptions: IRequestOptions): Promise<IInitiativeModel>;
