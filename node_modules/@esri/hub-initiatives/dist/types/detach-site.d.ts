import { IRequestOptions } from "@esri/arcgis-rest-request";
/**
 * Remove the linkage between a site and an Initiative
 * Share the site to the Org's default Collaboration Group
 *
 * @export
 * @param {string} siteId
 * @param {*} collaborationGroupId
 * @param {IRequestOptions} requestOptions
 * @returns {Promise<any>}
 */
export declare function detachSiteFromInitiative(siteId: string, collaborationGroupId: any, requestOptions: IRequestOptions): Promise<any>;
