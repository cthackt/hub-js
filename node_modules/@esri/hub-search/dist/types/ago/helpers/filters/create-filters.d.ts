import { ISearchParams } from "../../params";
/**
 * Create filters object based on raw params like tags=a,b or tags=any(a,b)
 *
 * @param {ISearchParams} params
 * @returns {any}
 */
export declare function createFilters(params: ISearchParams): any;
export declare function generateFilter(values: string, filterDefinition: any): {
    fn: any;
    terms: string[];
    catalogDefinition: any;
};
